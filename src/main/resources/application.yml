spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/cafehub?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: worldcup7



  jpa:
    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        format_sql: true


logging.level:
  org.hibernate.SQL: debug


# 세션 저장소에 있는 사용자 세션 매 요청마다 1800초로 갱신
server:
  servlet:
    session:
      timeout: 1800

  port: 8090